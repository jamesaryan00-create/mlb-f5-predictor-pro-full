const test=require('node:test'),assert=require('node:assert/strict');const {metrics,totals,priorLogs,inning}=require('../lib/pitching-review');
test('FIP uses outs and explicit constant; missing stats stay missing',()=>{const t={outs:10,earnedRuns:1,hits:2,baseOnBalls:0,strikeOuts:4,homeRuns:1,hitBatsmen:0};const m=metrics(t,3);assert.equal(m.innings,'3.1');assert.equal(m.fip,4.5);assert.equal(m.kbb,null);assert.equal(metrics(t,null).fip,null);assert.equal(totals([{...t,hits:null}]),null);});
test('date cutoff excludes target and future games',()=>{const d={stats:[{type:{displayName:'gameLog'},splits:['2026-09-13','2026-09-14','2026-09-15'].map((date,i)=>({date,gameType:'R',game:{gamePk:i}}))}]};assert.equal(priorLogs(d,'2026-09-14').length,1);});
test('inning distinguishes earned runs and excludes mixed pitcher innings',()=>{const play={about:{inning:1,halfInning:'top',isComplete:true},matchup:{pitcher:{id:7}},count:{outs:3},runners:[{details:{isScoringEvent:true,responsiblePitcher:{id:7},earned:false}}]};const f={liveData:{plays:{allPlays:[play]}}};assert.deepEqual(inning(f,7,1),{outs:3,earnedRuns:0,runs:1});f.liveData.plays.allPlays.unshift({...play,matchup:{pitcher:{id:8}}});assert.equal(inning(f,7,1),null);});
